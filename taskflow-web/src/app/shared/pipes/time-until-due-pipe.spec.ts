import { TimeUntilDuePipe } from './time-until-due-pipe';

describe('TimeUntilDuePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeUntilDuePipe();
    expect(pipe).toBeTruthy();
  });
});
